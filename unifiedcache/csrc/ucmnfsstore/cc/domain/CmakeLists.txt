# if(BUILD_TESTS)
#     file(GLOB_RECURSE UCMNFSSTORE_TEST_SOURCE_FILES "./test/*.*")
#     add_executable(ucmnfsstore_test_runner tests/ucmnfsstore_test_runner.cc)
#     target_link_libraries(ucmnfsstore_test_runner PRIVATE ucmnfsstore.core gtest gmock)
#     gtest_discover_tests(ucmnfsstore_test.test)
# endif(BUILD_TESTS)

if(BUILD_TESTS)
    file(GLOB_RECURSE UCMNFSSTORE_TEST_SOURCE_FILES "./test/*.*")
    add_executable(ucmnfsstore.test ${UCMNFSSTORE_TEST_SOURCE_FILES})
    target_link_libraries(ucmnfsstore.test PRIVATE ucmnfsstore.core gtest mockcpp)
    target_include_directories(ucmnfsstore.test PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/test)
    gtest_discover_tests(ucmnfsstore.test)
endif(BUILD_TESTS)

